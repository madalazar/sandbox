# Release Notes

## 1.0.0

- Initial Margo package for Docker Compose based RT benchmark workloads
- Added cyclictest compose component
- Added stress compose component
- Added configurable runtime parameters (CPU pinning, workers, duration, image references)
